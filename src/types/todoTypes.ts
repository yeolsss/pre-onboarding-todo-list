export interface Todo {
  id: number;
  todo: string;
}

export enum BUTTON_TYPES {
  SUBMIT = "submit",
  RESET = "reset",
  BUTTON = "button",
}

export enum BUTTON_TITLE {
  ADD = "Add",
  DELETE = "Delete",
}
